# Leave blank for local dev (Vite proxy handles /api -> localhost:5000).
# For production, set this to your deployed backend URL, e.g.:
# VITE_API_URL=https://mediflow-api.onrender.com/api
VITE_API_URL=
